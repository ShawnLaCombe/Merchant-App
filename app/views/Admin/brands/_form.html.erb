<%= simple_form_for([:admin, @brand]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name class:"btn btn-info"%>
  </div>

  <div class="form-actions">
    <%= f.button :submit class:"btn btn-info"%>
  </div>
<% end %>
